# Project-wide Gradle settings
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.daemon=true

# AndroidX package structure
android.useAndroidX=true
android.enableJetifier=true

# Kotlin code style
kotlin.code.style=official

# Kotlin version
kotlin.version=1.9.22

# Suppress Kotlin version compatibility check
suppressKotlinVersionCompatibilityCheck=1.9.22

# Android build features (non-deprecated properties)
android.defaults.buildfeatures.aidl=false
android.defaults.buildfeatures.renderscript=false
android.defaults.buildfeatures.resvalues=false
android.defaults.buildfeatures.shaders=false

# R class optimization
android.nonTransitiveRClass=true
android.nonFinalResIds=false

# Gradle optimization (Gradle 8.13 compatible)
org.gradle.configuration-cache=false
org.gradle.configuration-cache.problems=warn
org.gradle.warning.mode=all

# Android SDK settings
android.suppressUnsupportedCompileSdk=34

# Build performance
org.gradle.workers.max=8

# Removed deprecated properties:
# - android.enableSdkXmlParsing (no longer needed in Gradle 8+)
# - android.suppressUnsupportedSdkVersionWarning (deprecated)
# - org.gradle.unsafe.configuration-cache (use org.gradle.configuration-cache.problems instead)
# - org.gradle.build.cache.enabled (use org.gradle.caching instead)
