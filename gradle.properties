# Project-wide Gradle settings
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.daemon=true

# AndroidX package structure
android.useAndroidX=true
android.enableJetifier=true

# Kotlin code style
kotlin.code.style=official

# Kotlin version
kotlin.version=1.9.22

# Suppress Kotlin version compatibility check
suppressKotlinVersionCompatibilityCheck=1.9.22

# Android build features (non-deprecated properties)
android.defaults.buildfeatures.aidl=false
android.defaults.buildfeatures.renderscript=false
android.defaults.buildfeatures.resvalues=false
android.defaults.buildfeatures.shaders=false

# R class optimization
android.nonTransitiveRClass=true
android.nonFinalResIds=false

# Gradle optimization (Gradle 8.13 compatible)
org.gradle.configuration-cache=false
org.gradle.configuration-cache.problems=warn

# Suppress all deprecation warnings from Android Gradle Plugin
org.gradle.warning.mode=none

# Android SDK settings
android.suppressUnsupportedCompileSdk=34

# Build performance
org.gradle.workers.max=8

# Suppress SDK XML version warnings
android.experimental.lint.version=8.3.2
android.lint.useK2Uast=false

# Disable unused lint checks for faster builds
android.enableR8.fullMode=false

# Suppress AGP internal Boolean property deprecation warnings
org.gradle.internal.deprecation.details=false
org.gradle.internal.deprecation.stacktrace=false
