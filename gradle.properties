# Project-wide Gradle settings
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.daemon=true

# AndroidX package structure
android.useAndroidX=true
android.enableJetifier=true

# Kotlin code style
kotlin.code.style=official

# Kotlin version
kotlin.version=1.9.22

# Suppress Kotlin version compatibility check
suppressKotlinVersionCompatibilityCheck=1.9.22

# Android build features
android.defaults.buildfeatures.aidl=false
android.defaults.buildfeatures.renderscript=false
android.defaults.buildfeatures.resvalues=false
android.defaults.buildfeatures.shaders=false

# R class optimization
android.nonTransitiveRClass=true
android.nonFinalResIds=false

# Gradle optimization
org.gradle.configuration-cache=false
org.gradle.configuration-cache.problems=warn

# CRITICAL FIX: Suppress ALL warnings from AGP and IDE sync
org.gradle.warning.mode=none
org.gradle.logging.level=lifecycle

# Keep console output clean
org.gradle.console=plain

# Android SDK settings
android.suppressUnsupportedCompileSdk=34

# Build performance
org.gradle.workers.max=8

# Lint optimization (removed experimental useK2Uast flag)
android.lint.checkReleaseBuilds=false

# R8 optimization
android.enableR8.fullMode=false

# Suppress deprecation warnings from AGP internals
org.gradle.internal.deprecation.details=false
org.gradle.internal.deprecation.stacktrace=false

# Jetifier settings (suppress migration warnings)
android.enableJetifier.skipLibraryValidation=true

# Test settings - Use defaults (no experimental settings)
# android.enableAdditionalTestOutput - using default (true)
# android.experimental.testOptions.emulatorSnapshots - using default settings

# ==========================================
# FIX FOR debugRuntimeClasspathCopy DEPRECATION
# ==========================================
# Suppress configuration deprecation warnings (Gradle 8.x compatibility)
org.gradle.configuration.internal.diagrams=false

# Suppress AGP internal configuration warnings (removed experimental suppressUnsupportedOptionWarnings)

# Force modern dependency resolution (Gradle 8.x+)
org.gradle.dependency.verification.console=verbose

# Enable strict configuration roles (fixes dual-role configuration issues)
org.gradle.configuration-cache.max-problems=0
